{
  "name": "tokens-per-ticket-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Session registry: which ticket each Claude Code session is on, read by the LiteLLM gateway plugin.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
